import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export const CustomCallScreen = () => {
  return (
    <View style={{flex: 1, backgroundColor: '#fff'}}>
      <Text>CustomCallScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
